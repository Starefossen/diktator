# Frontend environment variables for local development
# Copy this file to .env.local and adjust as needed

# API URL for backend
NEXT_PUBLIC_API_URL=http://localhost:8080

# ============================================================
# Authentication Configuration
# ============================================================

# Authentication mode: 'mock' for development, 'oidc' for production
# - mock: Simple development mode with fake authentication (any credentials work)
# - oidc: Production mode with real OIDC provider (Zitadel, Keycloak, Auth0, etc.)
NEXT_PUBLIC_AUTH_MODE=mock

# ============================================================
# OIDC Configuration (only needed when NEXT_PUBLIC_AUTH_MODE=oidc)
# ============================================================

# OIDC issuer URL - your identity provider's base URL
# Examples:
#   - Zitadel: https://your-instance.zitadel.cloud
#   - Keycloak: https://keycloak.example.com/realms/your-realm
#   - Auth0: https://your-tenant.auth0.com
#   - Google: https://accounts.google.com
# NEXT_PUBLIC_OIDC_ISSUER_URL=https://your-identity-provider.com

# Client ID for this application (obtained from your OIDC provider)
# NEXT_PUBLIC_OIDC_CLIENT_ID=your-client-id

# Redirect URI after successful login (defaults to /auth/callback)
# Must be registered in your OIDC provider's allowed redirect URIs
# NEXT_PUBLIC_OIDC_REDIRECT_URI=http://localhost:3000/auth/callback

# Post-logout redirect URI (defaults to root /)
# Must be registered in your OIDC provider's allowed logout redirect URIs
# NEXT_PUBLIC_OIDC_POST_LOGOUT_REDIRECT_URI=http://localhost:3000

# Scopes to request from OIDC provider (defaults to "openid profile email")
# NEXT_PUBLIC_OIDC_SCOPES=openid profile email

# ============================================================
# Mock User Configuration (only used when NEXT_PUBLIC_AUTH_MODE=mock)
# ============================================================

# These values are used for development/testing only
# In mock mode, the actual login form credentials are ignored
NEXT_PUBLIC_MOCK_USER_ID=mock-user-12345
NEXT_PUBLIC_MOCK_USER_EMAIL=dev@localhost
NEXT_PUBLIC_MOCK_USER_NAME=Development User
